import 'package:flutter/material.dart';

class SmartDeviceCardWidget extends StatelessWidget {
  const SmartDeviceCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
